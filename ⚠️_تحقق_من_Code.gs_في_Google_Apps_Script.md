# ⚠️ تحقق من Code.gs في Google Apps Script - خطوات دقيقة

## 🔴 المشكلة الحالية:

السجلات تظهر:
```
❌ [BACKEND] ===== خطأ جذري: addClinicVisitToSheet تم استدعاؤها بدون معاملات =====
```

**⚠️ لا توجد رسائل `[CODE.GS]` أو `[DOPOST]` في السجلات!**

هذا يعني أن **`Code.gs` في Google Apps Script لم يتم تحديثه بشكل صحيح**.

---

## ✅ خطوات التحقق الدقيقة:

### 1️⃣ التحقق من `Code.gs` في Google Apps Script:

1. **افتح Google Apps Script**: [script.google.com](https://script.google.com)
2. **افتح ملف `Code.gs`** (اضغط عليه في القائمة الجانبية)
3. **ابحث عن السطر التالي** (اضغط Ctrl+F):
   ```
   🚀 [DOPOST] ===== doPost تم استدعاؤها =====
   ```
   
   **⚠️ مهم جداً:**
   - ✅ **إذا وجدته**: الكود محدث - انتقل للخطوة 2
   - ❌ **إذا لم تجده**: الكود لم يتم تحديثه - يجب نسخه الآن

---

### 2️⃣ إذا لم تجد `🚀 [DOPOST]`:

**هذا يعني أن الكود لم يتم نسخه بشكل صحيح!**

#### خطوات النسخ الصحيحة:

1. **افتح `Backend/Code.gs`** من مجلد المشروع المحلي
2. **حدد الكود بالكامل**:
   - اضغط **Ctrl+A** (أو Cmd+A على Mac)
   - تأكد من أن الكود كله محدد (يجب أن ترى كل السطور مظللة)
3. **انسخ الكود**:
   - اضغط **Ctrl+C** (أو Cmd+C على Mac)
   - تأكد من أن الكود تم نسخه (يمكنك لصقه في Notepad للتحقق)
4. **ارجع إلى Google Apps Script**
5. **افتح `Code.gs`**
6. **حدد الكود القديم بالكامل**:
   - اضغط **Ctrl+A** (أو Cmd+A على Mac)
   - تأكد من أن الكود كله محدد
7. **احذف الكود القديم**:
   - اضغط **Delete** (أو Backspace)
   - تأكد من أن الملف أصبح فارغاً
8. **الصق الكود الجديد**:
   - اضغط **Ctrl+V** (أو Cmd+V على Mac)
   - تأكد من أن الكود تم لصقه بالكامل
9. **احفظ الملف**:
   - اضغط **Ctrl+S** (أو Cmd+S على Mac)
   - أو اضغط على أيقونة **💾 Save** في الأعلى
   - **⚠️ مهم جداً**: تأكد من أن الملف محفوظ (لا توجد علامة `*` بجانب اسم الملف)

---

### 3️⃣ التحقق مرة أخرى:

بعد النسخ واللصق، تحقق مرة أخرى:

1. **ابحث عن** (Ctrl+F):
   ```
   🚀 [DOPOST] ===== doPost تم استدعاؤها =====
   ```
   - ✅ **يجب أن تجده الآن**

2. **ابحث عن** (Ctrl+F):
   ```
   case 'addClinicVisit':
   ```
   - ✅ **يجب أن تجد**: `Logger.log('🚀 [CODE.GS] ===== addClinicVisit action تم استدعاؤها =====');`
   - ✅ **يجب أن تجد**: `result = addClinicVisitToSheet(visitDataToUse);`

---

### 4️⃣ تحديث `Clinic.gs` أيضاً:

1. **افتح `Backend/Clinic.gs`** من مجلد المشروع المحلي
2. **حدد الكود بالكامل** (Ctrl+A)
3. **انسخ** (Ctrl+C)
4. **ارجع إلى Google Apps Script**
5. **افتح `Clinic.gs`** (أو أنشئه إذا لم يكن موجوداً)
6. **حدد الكود القديم بالكامل** (Ctrl+A)
7. **احذف** (Delete)
8. **الصق الكود الجديد** (Ctrl+V)
9. **احفظ** (Ctrl+S)

---

### 5️⃣ إعادة نشر Web App:

1. **Deploy** → **Manage deployments**
2. **اضغط على Edit** (أيقونة القلم ✏️)
3. **⚠️ مهم جداً جداً**: 
   - تأكد من أن **Version** = **New**
   - **ليس** "Use existing version"
   - إذا كان "Use existing version"، غيّره إلى **New**
4. **اضغط Deploy**
5. **انتظر 15-20 ثانية** بعد النشر قبل الاختبار

---

### 6️⃣ التحقق من النشر:

1. **Deploy** → **Manage deployments**
2. **تحقق من "Last deployed"**:
   - يجب أن يكون الوقت الحالي (بعد التحديث)
   - إذا كان الوقت قديم، فالنشر لم يتم بشكل صحيح
3. **تحقق من "Version"**:
   - يجب أن يكون رقم جديد
   - إذا كان نفس الرقم القديم، فالنشر لم يتم بشكل صحيح

---

## ✅ التحقق من التحديث:

بعد التحديث، عند تسجيل زيارة جديدة يجب أن ترى في Execution Logs:

```
🚀 [DOPOST] ===== doPost تم استدعاؤها =====
🚀 [DOPOST] الوقت: 2026-01-27T22:49:45.928Z
🔍 [CODE.GS] postData.action: "addClinicVisit"
🔍 [CODE.GS] postData.data exists: true
🚀 [CODE.GS] ===== addClinicVisit action تم استدعاؤها =====
✅ [CODE.GS] تم العثور على بيانات الزيارة، عدد الحقول: 15
🚀 [BACKEND] ===== addClinicVisitToSheet تم استدعاؤها =====
🚀 [BACKEND] عدد المعاملات: 1  ✅ (وليس 0)
✅ [BACKEND] visitData موجود، عدد الحقول: 15
```

---

## ⚠️ إذا لم تظهر رسائل `[CODE.GS]` بعد كل هذا:

### الحل البديل: حذف وإعادة إنشاء `Code.gs`

1. **في Google Apps Script**:
   - اضغط بزر الماوس الأيمن على `Code.gs`
   - اختر **Delete**
   - أكد الحذف

2. **أنشئ ملف جديد**:
   - اضغط على **+** بجانب "Files"
   - اختر **Script**
   - اسمه `Code.gs`

3. **انسخ الكود من `Backend/Code.gs` المحلي**:
   - افتح `Backend/Code.gs` من مجلد المشروع
   - انسخ الكود بالكامل (Ctrl+A ثم Ctrl+C)
   - الصق في Google Apps Script (Ctrl+V)
   - احفظ (Ctrl+S)

4. **أعد نشر Web App**:
   - Deploy → Manage deployments → Edit
   - اختر **New version**
   - Deploy
   - انتظر 15-20 ثانية

---

## 📝 نقاط التحقق النهائية:

- [ ] `Code.gs` في Google Apps Script يحتوي على `🚀 [DOPOST] ===== doPost تم استدعاؤها =====`
- [ ] `Code.gs` في Google Apps Script يحتوي على `🚀 [CODE.GS] ===== addClinicVisit action تم استدعاؤها =====`
- [ ] `case 'addClinicVisit'` يحتوي على `result = addClinicVisitToSheet(visitDataToUse);`
- [ ] الملف محفوظ (لا توجد علامة `*`)
- [ ] Web App تم نشره مع **New version**
- [ ] "Last deployed" يظهر الوقت الحالي
- [ ] انتظرت 15-20 ثانية بعد النشر
- [ ] السجلات تظهر رسائل `[CODE.GS]` و `[DOPOST]`

---

## 🔍 ملاحظة مهمة:

إذا كنت تختبر الدالة مباشرة من محرر Google Apps Script (من خلال اختيار `addClinicVisitToSheet` من القائمة المنسدلة)، فهذا لن يعمل لأن الدالة تحتاج إلى معاملات.

**يجب أن تختبر من خلال التطبيق الفعلي** (تسجيل زيارة جديدة في التطبيق)، وليس من محرر Google Apps Script.

---

**آخر تحديث:** 2026-01-27
