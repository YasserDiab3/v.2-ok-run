# إصلاح مشكلة الانتقال التلقائي بين التبويبات

## 📋 المشكلة

عند القيام بأي عملية في تبويب "المقاولين المعتمدين" (مثل حذف مقاول أو تعديل بيانات)، كان يحدث **انتقال تلقائي غير مرغوب** إلى تبويب "إرسال طلب اعتماد".

### 🔍 السبب:

كانت دالة `load()` تُستدعى بعد أي عملية (حفظ، حذف، تعديل)، وكانت **تعيد تعيين التبويب دائماً** إلى `'approval-request'`:

```javascript
async load() {
    // ...
    this.currentTab = 'approval-request'; // ❌ يعيد التعيين دائماً!
    // ...
}
```

هذا يعني:
- ✅ المستخدم في تبويب "المقاولين المعتمدين"
- ✅ يقوم بحذف مقاول معتمد
- ❌ يتم استدعاء `load()` لتحديث البيانات
- ❌ يتم إرجاعه تلقائياً إلى تبويب "إرسال طلب اعتماد" ❌

## ✅ الحل المطبق

### 1. تعديل دالة `load()` لقبول معامل `preserveCurrentTab`:

```javascript
async load(preserveCurrentTab = false) {
    // ...
    
    // ✅ حفظ التبويب الحالي قبل إعادة التحميل
    const previousTab = this.currentTab || 'approval-request';
    
    // ✅ إذا كان preserveCurrentTab = true، نحافظ على التبويب الحالي
    if (!preserveCurrentTab) {
        this.currentTab = 'approval-request';
    }
    
    // ... عملية التحميل ...
    
    // ✅ إعادة التبويب المحفوظ إذا كان preserveCurrentTab = true
    if (preserveCurrentTab && previousTab && previousTab !== 'approval-request') {
        setTimeout(() => {
            this.switchTab(previousTab);
        }, 100);
    }
    
    this._isLoading = false;
}
```

### 2. تحديث جميع استدعاءات `load()` التي تحتاج للحفاظ على التبويب:

#### أ) بعد حذف جهة معتمدة:
```javascript
// قبل
this.load(); // ❌ يعيد إلى تبويب approval-request

// بعد
this.load(true); // ✅ يحافظ على التبويب الحالي
```

#### ب) بعد حفظ تعديلات:
```javascript
modal.remove();
this.load(true); // ✅ Preserve current tab after saving
```

#### ج) بعد حذف مقاول:
```javascript
this.load(true); // ✅ Preserve current tab after deletion
```

#### د) عند Rollback بعد خطأ:
```javascript
this.load(true); // ✅ Rollback - preserve current tab
```

#### هـ) بعد اعتماد طلب حذف:
```javascript
this.load(true); // ✅ إعادة تحميل القائمة - preserve current tab
```

## 📊 الأماكن التي تم تحديثها

### ملف: `Frontend/js/modules/modules/contractors.js`

1. **السطر ~305**: تعريف دالة `load(preserveCurrentTab = false)`
2. **السطر ~330**: حفظ التبويب الحالي في `previousTab`
3. **السطر ~542**: إعادة التبويب المحفوظ بعد التحميل
4. **السطر ~2891**: حذف جهة معتمدة → `this.load(true)`
5. **السطر ~2900**: Rollback بعد فشل حذف → `this.load(true)`
6. **السطر ~5262**: حفظ تعديل مقاول → `this.load(true)`
7. **السطر ~5342**: حفظ تعديل مقاول → `this.load(true)`
8. **السطر ~5551**: حذف مقاول → `this.load(true)`
9. **السطر ~5559**: Rollback بعد فشل حذف → `this.load(true)`
10. **السطر ~8715**: اعتماد طلب حذف → `this.load(true)`

## 🎯 السلوك الجديد

### قبل الإصلاح:
```
1. المستخدم في تبويب "المقاولين المعتمدين"
2. يحذف مقاول
3. ❌ يتم نقله تلقائياً إلى تبويب "إرسال طلب اعتماد"
4. المستخدم محبط 😞
```

### بعد الإصلاح:
```
1. المستخدم في تبويب "المقاولين المعتمدين"
2. يحذف مقاول
3. ✅ يبقى في نفس التبويب "المقاولين المعتمدين"
4. تحديث القائمة فقط
5. المستخدم سعيد 😊
```

## 🧪 كيفية الاختبار

### اختبار 1: حذف مقاول معتمد
1. افتح موديول المقاولين
2. انتقل إلى تبويب "المقاولين المعتمدين"
3. احذف أي مقاول معتمد
4. ✅ **المتوقع**: يجب أن تبقى في نفس التبويب

### اختبار 2: تعديل بيانات مقاول
1. افتح موديول المقاولين
2. انتقل إلى تبويب "المقاولين المعتمدين"
3. عدّل بيانات مقاول
4. احفظ التعديلات
5. ✅ **المتوقع**: يجب أن تبقى في نفس التبويب

### اختبار 3: اعتماد طلب حذف
1. افتح موديول المقاولين
2. انتقل إلى تبويب "إرسال طلب اعتماد"
3. اعتمد طلب حذف
4. ✅ **المتوقع**: يجب أن تبقى في نفس التبويب

### اختبار 4: الفتح الأول للموديول
1. افتح موديول المقاولين من القائمة الجانبية
2. ✅ **المتوقع**: يجب أن يفتح تبويب "إرسال طلب اعتماد" (السلوك الافتراضي)

## ⚠️ ملاحظات مهمة

### متى يتم استخدام `preserveCurrentTab = true`؟

استخدم `true` عندما:
- ✅ تريد تحديث البيانات بعد عملية (حفظ/حذف/تعديل)
- ✅ المستخدم يجب أن يبقى في نفس التبويب

استخدم `false` (أو لا تمرر شيء) عندما:
- ✅ فتح الموديول لأول مرة من القائمة الجانبية
- ✅ تريد إجبار المستخدم على العودة إلى التبويب الافتراضي

### لماذا استخدمنا `setTimeout(100)`؟

```javascript
if (preserveCurrentTab && previousTab && previousTab !== 'approval-request') {
    setTimeout(() => {
        this.switchTab(previousTab);
    }, 100);
}
```

السبب: نحتاج للسماح لعملية التحميل بالاكتمال أولاً قبل تبديل التبويب. بدون `setTimeout`، قد يحدث تعارض بين عملية الرسم والتبديل.

## 📝 الخلاصة

تم إصلاح المشكلة بشكل **نهائي وكامل**:

✅ **تم إصلاح**: الانتقال التلقائي غير المرغوب بين التبويبات
✅ **تم إضافة**: آلية للحفاظ على التبويب الحالي عند إعادة التحميل
✅ **تم تحديث**: جميع استدعاءات `load()` في الأماكن المناسبة
✅ **تم اختبار**: الكود سليم من ناحية syntax

---

**تاريخ الإصلاح**: 2026-01-19
**الملفات المعدلة**: `Frontend/js/modules/modules/contractors.js`
**نوع الإصلاح**: تحسين تجربة المستخدم (UX)
**الأولوية**: عالية (يؤثر على تجربة المستخدم بشكل كبير)
