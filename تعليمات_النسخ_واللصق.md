# 📋 تعليمات نسخ ولصق الكود في Google Apps Script

## ✅ الملفات الجاهزة للصق:

تم إنشاء ملفين نصيين يحتويان على الكود الكامل:
1. **`Code.gs_للصق.txt`** - الكود الكامل لملف `Code.gs`
2. **`Clinic.gs_للصق.txt`** - الكود الكامل لملف `Clinic.gs`

---

## 📝 الخطوات:

### 1. فتح Google Apps Script:
- اذهب إلى [script.google.com](https://script.google.com)
- أو من Google Sheets: **Extensions** → **Apps Script**

### 2. تحديث ملف `Code.gs`:
1. **افتح ملف `Code.gs`** في Google Apps Script
2. **حدد الكود بالكامل** (Ctrl+A)
3. **احذف الكود القديم** (Delete)
4. **افتح ملف `Code.gs_للصق.txt`** من مجلد المشروع
5. **انسخ الكود بالكامل** (Ctrl+A ثم Ctrl+C)
6. **الصق في Google Apps Script** (Ctrl+V)
7. **احفظ** (Ctrl+S)

### 3. تحديث ملف `Clinic.gs`:
1. **افتح ملف `Clinic.gs`** في Google Apps Script (أو أنشئه إذا لم يكن موجوداً)
2. **حدد الكود بالكامل** (Ctrl+A)
3. **احذف الكود القديم** (Delete)
4. **افتح ملف `Clinic.gs_للصق.txt`** من مجلد المشروع
5. **انسخ الكود بالكامل** (Ctrl+A ثم Ctrl+C)
6. **الصق في Google Apps Script** (Ctrl+V)
7. **احفظ** (Ctrl+S)

### 4. إعادة نشر Web App:
1. **Deploy** → **Manage deployments**
2. اضغط على **Edit** (أيقونة القلم ✏️)
3. **⚠️ مهم جداً:** اختر **New version** (وليس استخدام نفس الإصدار)
4. اضغط **Deploy**
5. **انسخ رابط الويب الجديد** (إذا تغير)

---

## ✅ نقاط التحقق بعد التحديث:

- [ ] `Code.gs` محدث في Google Apps Script
- [ ] `Clinic.gs` محدث في Google Apps Script
- [ ] Web App تم إعادة نشره (New version)
- [ ] السجلات تظهر `🚀 [CODE.GS]` عند تسجيل زيارة جديدة
- [ ] السجلات تظهر `🚀 [DOPOST]` عند تسجيل زيارة جديدة
- [ ] `addClinicVisitToSheet` يتم استدعاؤها بمعامل واحد (وليس 0)
- [ ] `createdBy` موجود في السجلات وقيمته صحيحة

---

## 🔍 ما الذي يجب أن تراه بعد التحديث:

عند تسجيل زيارة جديدة، يجب أن ترى في Execution Logs:

```
🚀 [DOPOST] ===== doPost تم استدعاؤها =====
🔍 [CODE.GS] postData.action: "addClinicVisit"
🔍 [CODE.GS] postData.data exists: true
🚀 [CODE.GS] ===== addClinicVisit action تم استدعاؤها =====
✅ [CODE.GS] تم العثور على بيانات الزيارة، عدد الحقول: 15
🚀 [BACKEND] ===== addClinicVisitToSheet تم استدعاؤها =====
🚀 [BACKEND] عدد المعاملات: 1  ✅ (وليس 0)
✅ [BACKEND] visitData موجود، عدد الحقول: 15
🔍 [BACKEND] visitData.createdBy المستلم من Frontend: "اسم المستخدم"
✅ [BACKEND] createdBy النهائي المحفوظ في قاعدة البيانات: اسم المستخدم
```

---

**آخر تحديث:** 2026-01-27
