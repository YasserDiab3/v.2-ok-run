# دليل إعداد Google Maps للاستقرار الأفضل

## 📋 نظرة عامة

التطبيق يدعم حالياً نظامين للخرائط:
1. **Google Maps** (الأفضل - مستقر وموثوق) ⭐
2. **OpenStreetMap/Leaflet** (بديل مجاني - قد يواجه مشاكل في الاستقرار)

## ✅ لماذا Google Maps أفضل؟

| الميزة | Google Maps | OpenStreetMap |
|--------|-------------|---------------|
| **الاستقرار** | ⭐⭐⭐⭐⭐ ممتاز | ⭐⭐⭐ جيد |
| **السرعة** | ⭐⭐⭐⭐⭐ سريع جداً | ⭐⭐⭐ متوسط |
| **الدقة** | ⭐⭐⭐⭐⭐ دقيق جداً | ⭐⭐⭐ جيد |
| **التحديثات** | ⭐⭐⭐⭐⭐ مستمرة | ⭐⭐⭐ محدودة |
| **دعم الأقمار الصناعية** | ✅ متوفر | ❌ محدود |
| **دعم Street View** | ✅ متوفر | ❌ غير متوفر |
| **التكلفة** | 💰 مجاني حتى 28,000 طلب/شهر | 💰 مجاني تماماً |

## 🔧 خطوات الإعداد

### الخطوة 1: الحصول على مفتاح Google Maps API

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد أو اختر مشروع موجود
3. اذهب إلى **APIs & Services** > **Library**
4. ابحث عن **Maps JavaScript API** وفعّله
5. اذهب إلى **APIs & Services** > **Credentials**
6. انقر على **Create Credentials** > **API Key**
7. انسخ المفتاح (سيبدو مثل: `AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)

### الخطوة 2: إعداد قيود المفتاح (أمان)

1. في صفحة **Credentials**، انقر على المفتاح الذي أنشأته
2. في **Application restrictions**:
   - اختر **HTTP referrers (web sites)**
   - أضف نطاق موقعك (مثال: `https://yourdomain.com/*`)
   - أضف نطاق Netlify إذا كنت تستخدمه (مثال: `https://*.netlify.app/*`)
3. في **API restrictions**:
   - اختر **Restrict key**
   - اختر فقط **Maps JavaScript API**
4. احفظ التغييرات

### الخطوة 3: إضافة المفتاح إلى التطبيق

#### الطريقة 1: من خلال الإعدادات (إن وجدت)

1. افتح التطبيق
2. اذهب إلى **الإعدادات** > **إعدادات Google**
3. أدخل مفتاح API في حقل **Google Maps API Key**
4. احفظ التغييرات

#### الطريقة 2: من خلال الكود (يدوياً)

افتح ملف `Frontend/js/modules/app-utils.js` وابحث عن:

```javascript
googleConfig: {
    maps: {
        enabled: true,
        apiKey: '' // ← ضع المفتاح هنا
    }
}
```

أو من خلال Console في المتصفح:

```javascript
// في Console المتصفح (F12)
AppState.googleConfig.maps.apiKey = 'YOUR_API_KEY_HERE';
localStorage.setItem('hse_google_config', JSON.stringify(AppState.googleConfig));
location.reload(); // إعادة تحميل الصفحة
```

## 🔍 التحقق من الإعداد

بعد إضافة المفتاح:

1. افتح التطبيق
2. اذهب إلى تبويب **الخريطة**
3. إذا ظهرت خريطة Google Maps بدلاً من OpenStreetMap، فالإعداد نجح ✅
4. افتح Console (F12) وتحقق من عدم وجود أخطاء

## ⚠️ ملاحظات مهمة

### حدود الاستخدام المجاني

- **28,000 طلب/شهر** مجانياً
- بعد ذلك: **$7 لكل 1,000 طلب إضافي**
- للاستخدام العادي (مئات المستخدمين يومياً)، الحد المجاني كافٍ تماماً

### الأمان

- **لا تشارك مفتاح API** في الكود العام
- استخدم **قيود HTTP referrers** دائماً
- راقب الاستخدام من Google Cloud Console

### التكلفة

- الاستخدام العادي: **مجاني تماماً**
- الاستخدام الكبير: **تكلفة منخفضة جداً** (حوالي $0.007 لكل 1,000 طلب)

## 🆚 مقارنة الاستقرار

### OpenStreetMap (الحالي)
- ❌ قد يواجه أخطاء 503 (Service Unavailable)
- ❌ بطء في التحميل أحياناً
- ❌ جودة الصور أقل
- ✅ مجاني تماماً

### Google Maps (المُوصى به)
- ✅ استقرار عالي جداً (99.9% uptime)
- ✅ سرعة تحميل ممتازة
- ✅ جودة صور عالية
- ✅ دعم أفضل للأجهزة المحمولة
- ✅ ميزات إضافية (Street View، Satellite، Terrain)
- 💰 مجاني حتى 28,000 طلب/شهر

## 🔄 التبديل التلقائي

التطبيق مصمم للتبديل التلقائي:
- إذا كان هناك مفتاح Google Maps API → يستخدم Google Maps
- إذا لم يكن هناك مفتاح → يستخدم OpenStreetMap تلقائياً

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من أن المفتاح صحيح
2. تحقق من قيود HTTP referrers
3. تحقق من تفعيل Maps JavaScript API
4. راجع Console للأخطاء (F12)

## 📊 إحصائيات الاستخدام

يمكنك مراقبة الاستخدام من:
- Google Cloud Console > APIs & Services > Dashboard
- ستجد إحصائيات مفصلة عن عدد الطلبات والتكلفة

---

**ملاحظة**: بعد إضافة المفتاح، سيتم استخدام Google Maps تلقائياً في المرة القادمة التي تفتح فيها الخريطة.
